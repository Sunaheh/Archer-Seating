<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Chore Tracker</title>

<meta name="mobile-web-app-capable" content="yes">
<meta name="theme-color" content="#fffbe6">

<style>
  html, body {
    height: 100%;
    margin: 0;
    font-family: 'Comic Sans MS', Arial, sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    background: #fffbe6;
    text-align: center;
  }

  #container {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: center;
    height: 100vh;
    width: 100vw;
    padding: 5vh 5vw;
    box-sizing: border-box;
  }

  .line {
    margin: 0;
    padding: 0;
    text-align: center;
  }

  #today {
    font-size: 8vw;
    color: #333;
  }

  #kid-card {
    flex-grow: 1;
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    border-radius: 20px;
    overflow: hidden;
  }

  #kid {
    font-weight: bold;
    color: #fff;
    text-align: center;
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
  }

  #mia { background-color: #ff6f91; }
  #sienna { background-color: #6fb1fc; }

  #next {
    font-size: 6vw;
    color: #333;
  }
</style>
</head>
<body>

<div id="container">
  <p id="today" class="line">Today</p>
  <div id="kid-card">
    <div id="kid" class="line"></div>
  </div>
  <p id="next" class="line">gets to sit next to Archer</p>
</div>

<script>
  const kids = [
    { name: "Mia", id: "mia" },
    { name: "Sienna", id: "sienna" }
  ];

  function updateKid() {
    const startDate = new Date("2025-08-30"); 
    const today = new Date();
    const diffTime = today - startDate;
    const diffDays = Math.floor(diffTime / (1000 * 60 * 60 * 24));

    const currentIndex = diffDays % kids.length; 
    const currentKid = kids[currentIndex];

    const kidDiv = document.getElementById("kid");
    const cardDiv = document.getElementById("kid-card");
    kidDiv.textContent = currentKid.name;
    cardDiv.id = currentKid.id; // apply background color

    fitText(kidDiv);
  }

  function fitText(element, maxHeightFraction = 0.8) {
    let fontSize = 200; 
    element.style.fontSize = fontSize + 'px';
    const parentHeight = element.parentElement.clientHeight * maxHeightFraction;
    while(element.scrollHeight > parentHeight && fontSize > 10){
      fontSize -= 2;
      element.style.fontSize = fontSize + 'px';
    }
  }

  // Initial update
  window.addEventListener('load', () => {
    updateKid();
    scheduleNextUpdate();
  });

  window.addEventListener('resize', updateKid);
  window.addEventListener('orientationchange', updateKid);

  // Schedule automatic update at midnight
  function scheduleNextUpdate() {
    const now = new Date();
    const tomorrow = new Date(now.getFullYear(), now.getMonth(), now.getDate() + 1);
    const msUntilMidnight = tomorrow - now;
    setTimeout(() => {
      updateKid();
      scheduleNextUpdate(); // schedule next update
    }, msUntilMidnight);
  }
</script>

</body>
</html>
