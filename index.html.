<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Archer Seating</title>
  <style>
    body {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      font-family: Arial, sans-serif;
      background: #fdf6e3;
      color: #333;
      text-align: center;
    }
    h1 {
      font-size: 3rem;
      margin: 0.5rem 0;
    }
    h2 {
      font-size: 6rem; /* 👈 Big focus on kids' names */
      margin: 0.5rem 0;
      font-weight: bold;
      color: #2c3e50;
    }
    h3 {
      font-size: 2rem;
      margin: 0.5rem 0;
    }
  </style>
</head>
<body>
  <h1>Today</h1>
  <h2 id="kid-name"></h2>
  <h3>gets to sit next to Archer!</h3>

  <script>
    const kids = ["Mia", "Sienna"];
    const startDate = new Date("2025-08-30"); // starting day for Mia
    const today = new Date();
    const diffDays = Math.floor((today - startDate) / (1000 * 60 * 60 * 24));
    const kidToday = kids[diffDays % kids.length];
    document.getElementById("kid-name").textContent = kidToday;
  </script>
</body>
</html>
